<!-- Partial head -->
<%- include('../partials/head.ejs')%>

    <title>Login | Focal Glow</title>
    <link rel="stylesheet" href="/css/styles-user-list.css">


    </head>

    <body>
        <%- include('../partials/header.ejs')%>

            <main class="container-users">
                <table id="customers">
                    <tr>
                        <th class="id">ID</th>
                        <th>Nombre</th>
                        <th>Apellido</th>
                        <th>Mail</th>
                        <th class="profile">Imagen de Perfil</th>
                        <th>Editar</th>
                        <th>Borrar</th>
                    </tr>
                    <% userList.forEach(e=> { %>
                        <tr>
                            <td>
                                <%= e.id%>
                            </td>
                            <td>
                                <%= e.name%>
                            </td>
                            <td>
                                <%= e.surname%>
                            </td>
                            <td>
                                <%= e.email%>
                            </td>
                            <td>
                                <img class = "img_Redonda" src="<%= e.profileImg%>">
                            </td>
                            <td><form action="/users/<%=e.id%>?_method=DELETE" method="POST">
                                <button type="submit"><i class="far fa-trash-alt"></i></button>
                            </form></td>
                           <td> <a href="/users/<%=e.id%>/edit">
                                <i class="far fa-edit"></i>
                            </a></td>

                        </tr>
                        <%})%>
            </main>
    </body>

    </html>